import { Text, View  } from 'react-native';
import { Base, Typography } from '../../styles'

export default function SearchForm({setIsLoggedIn}) { 
    return (
            <View style={Base.center}>
                <Text style={Typography.header2}>SearchForm</Text>
            </View>
    )
};