import { Text, View  } from 'react-native';
import { Base, Typography } from '../../styles'

export default function Test() { 
    return (
            <View style={Base.center}>
                <Text style={Typography.header2}>Test</Text>
            </View>
    )
};